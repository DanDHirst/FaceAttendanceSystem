@model FaceAttendance.Models.Lecturer

@{
    ViewData["Title"] = "Edit";
}
    <partial name="_AdminPartial" />
<h1>Edit</h1>

<h4>Lecturer</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ID" />
            <div class="form-group">
                <label asp-for="LecturerCode" class="control-label"></label>
                <input asp-for="LecturerCode" class="form-control" />
                <span asp-validation-for="LecturerCode" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="LecturerName" class="control-label"></label>
                <input asp-for="LecturerName" class="form-control" />
                <span asp-validation-for="LecturerName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
